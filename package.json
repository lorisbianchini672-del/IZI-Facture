{
  "name": "izi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "for f in server.js db.js index.js lib/*.js test/*.js scripts/*.js; do node --check \"$f\" || exit 1; done && echo 'Syntaxe : OK'"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.116.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemailer": "^6.9.16",
    "pdfkit": "^0.15.0",
    "stripe": "^17.5.0"
  }
}
